class Court {
  String id;
  String serviceId;
  String serviceName;
  String name;
  double price;
  bool status;
  DateTime? createdAt;

  Court(this.id, this.serviceId, this.serviceName, this.name, this.price, this.status,this.createdAt);

}