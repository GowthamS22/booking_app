class Service {
  String id;
  String name;
  int displayOrder;
  int active;
  String icon;
  DateTime? createdAt;

  Service(this.id, this.name, this.displayOrder, this.active, this.icon, this.createdAt);

}